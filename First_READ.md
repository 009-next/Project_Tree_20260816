# First_READ — Project_Tree の起動方法

審査員の方へ。**このファイルだけ読めばアプリを動かせます。**

Project_Tree は、建設・土木の現場資料（議事録・メール・現場写真）を読み込み、
時系列の台帳・5段階のストーリー・資料用イメージ図・2D/3Dモデルを1つの画面でつなぐアプリです。

---

起動方法は2通りあります。**方法A（exe）が最も簡単です。**

| | 方法A: exeで起動 | 方法B: Pythonで起動 |
|---|---|---|
| Pythonのインストール | **不要** | 必要（3.11以上） |
| パッケージのインストール | **不要** | `pip install` が必要 |
| 手順 | exeをダブルクリック | コマンドを3行実行 |

---

## 方法A: exe で起動する（Python不要・推奨）

1. **Releases ページから `Project_Tree_windows.zip` をダウンロード**
   → https://github.com/009-next/Project_Tree/releases
2. **zipを右クリック →「すべて展開」で展開する**
   （展開せずzipの中から直接起動すると、台帳ファイルを読み書きできず失敗します）
3. 展開したフォルダの中の **`Project_Tree.exe` をダブルクリック**

黒いコンソールウィンドウが開き、続いてブラウザが自動で開きます。

> Windows の SmartScreen が「WindowsによってPCが保護されました」と表示することがあります。
> 署名なしの個人開発アプリのためです。**「詳細情報」→「実行」** で起動できます。

終了するときは、黒いコンソールウィンドウを閉じてください。

---

## 方法B: Python で起動する

事前に **Python 3.11 以上** が入っていることだけ確認してください（`python --version`）。

```bash
git clone https://github.com/009-next/Project_Tree.git
cd Project_Tree
pip install -r requirements.txt
python run_app.py
```

ブラウザが自動で開き、**http://127.0.0.1:8010/projecttree** が表示されます。
開かない場合は、上記URLを手で開いてください。

> **どちらの方法でも APIキーは不要です。**
> デモ用のデータ（案件23件・資料112件・記録431件・イメージ図14枚・3Dモデル）を `ledger.db` に同梱しているので、
> 起動直後からすべての画面を触れます。LLMは呼ばれないため**費用は0円**です。

---

## 参考: exe を自分でビルドする場合

Releases のexeを使う場合は不要です。ソースから作り直したいときのみ実行してください。

```bash
pip install -r requirements.txt
pip install pyinstaller
python build_exe.py
```

`dist/Project_Tree/Project_Tree.exe` が出来上がります。
台帳（`ledger.db`）と生成物（`assets/`・`uploads/`）は、ビルド後に自動でexeの隣へコピーされます。

---

## 起動後に見ていただきたい順番

### 1. 案件を選ぶ（左の一覧）

**「河川護岸整備工事」** を選んでください。記録・イメージ図・3Dモデルが全部そろっている案件です。
（「東部橋梁補修工事」も同様にそろっています）

一覧には他の案件も並びますが、記録だけで画像やモデルが未生成のものが混ざっています。
**最初はこの2案件のどちらかを選んでください。**

### 2. 上段：5段階に整理された記録とイメージ図

資料から抽出した記録が、状況確認 → 現状の課題 → 試行錯誤 → 課題・変化 → 解決策の提案、
の5段階に自動で振り分けられています。各カードの絵は、その段階の状況をLLMが描いた図です。

| 操作 | 起きること |
|---|---|
| **カードを左クリック** | その段階の記録が時系列で開く。元の資料の原文まで戻れる |
| **カードを右クリック** | その段階の資料を md / pdf / word / pptx / xlsx で出力 |

### 3. 下段：2D/3Dモデルと進捗率

| 操作 | 起きること |
|---|---|
| **モデルの部位を左クリック** | 対応する段階のカードが光る（例: 護岸ブロック → 段階3） |
| **モデルを右クリック** | GLB / STL / DXF / SVG でダウンロード |
| 右上の「2D平面図を表示」 | 3D ⇄ 2D平面図を切り替え |
| 右上の「表示段階」スライダー | 指定した段階までの部材だけを表示（工事の進み方が見える） |

**このモデルの生成に Blender は不要です。** 台帳の部材データからローカルで組み立てています（費用0円）。
Blenderを起動している場合のみ、「進捗」パネル下部から任意で連携できます。

### 4. 「▶ 出力」ボタン（0件から作られる様子を見る場合）

左下の入力欄に資料を入れて「▶ 出力」を押すと、
取込 → 時系列作成 → 案件分類 → 段階整理 → モデル生成 → 進捗算出 → イメージ図 → 推論
までが一括で走ります。**この経路だけLLMを使うため、実行前に必ず概算費用のダイアログが出ます。**
費用をかけたくない場合は、ここでキャンセルしてください（同梱データの閲覧には影響しません）。

---

## LLM機能も試す場合（任意・費用が発生します）

同梱データを見るだけなら不要です。新しい資料を解析したい場合のみ設定してください。

画面右上の **「⚙ 設定」** からキーを入力できます（プロセスのメモリ上にのみ保持。ファイルには保存しません）。
環境変数でも設定できます。

```bash
# Anthropic を直接使う場合
set ANTHROPIC_API_KEY=sk-ant-...

# Orca Router（Anthropic SDK互換プロキシ）を使う場合
set ORCA_API_KEY=sk-orca-...
```

キーが未設定でも、アプリの起動・同梱データの閲覧・資料出力・モデル操作はすべて動きます。

---

## うまく動かないとき

| 症状 | 対処 |
|---|---|
| ブラウザが自動で開かない | http://127.0.0.1:8010/projecttree を手で開く |
| `ポート8010が使用中のため8011で起動しました` と出る | 表示された方のポート番号でアクセスする（起動時のログに正しいURLが出ます） |
| SmartScreenで起動できない（exe） | 「詳細情報」→「実行」を選ぶ |
| exeを起動しても何も起きない | zipを**展開してから**起動しているか確認する |
| `ModuleNotFoundError`（Python起動時） | `pip install -r requirements.txt` を実行したか確認する |
| 画面は出るが案件が1件も無い | `ledger.db` が exe / `run_app.py` と同じフォルダにあるか確認する（同梱しています） |
| イメージ図や3Dモデルが表示されない | いったんアプリを終了し、起動し直す（起動時にファイルの場所を自動補正します） |

終了するには、起動したコンソールウィンドウを閉じるか `Ctrl+C` を押してください。

---

## 動作を確認した環境

- Windows 11 / Python 3.13.14
- ブラウザ: Microsoft Edge・Google Chrome

Python 3.11 以上であれば動作します。macOS / Linux でも同じ手順で起動できます
（`set` の代わりに `export` を使ってください）。

---

## 中身をもう少し知りたい方へ

| ファイル | 内容 |
|---|---|
| [README.md](README.md) | ファイル構成・設計方針 |
| [紹介記事_Project_Tree.md](紹介記事_Project_Tree.md) | 設計の狙いと、開発中に見つかった実バグの話 |
| [Project_Tree_プレゼン資料.pptx](Project_Tree_プレゼン資料.pptx) | プレゼン資料（10枚） |
| [scratch_out/demo_video/](scratch_out/demo_video/) | デモ動画（約2分） |
