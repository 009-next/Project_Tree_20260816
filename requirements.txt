# Project_Tree 実行に必要なパッケージ
# インストール: pip install -r requirements.txt

# Webアプリ本体
fastapi==0.135.3
uvicorn==0.44.0
pydantic==2.12.5

# LLM 呼び出し（APIキーを設定した場合のみ使用。未設定でもアプリは起動する）
anthropic==0.121.0

# 2D/3Dモデル生成（Blender不要・ローカル生成）
trimesh==4.12.2
ezdxf==1.4.4
numpy==2.4.3

# 資料出力（pptx / word / excel）と画像処理
python-pptx==1.0.2
python-docx==1.2.0
openpyxl==3.1.5
Pillow==12.2.0

# PDFの読み取り・出力
PyMuPDF==1.27.2.2

# CIM連携（IFC書き出し。無くてもアプリは起動する）
ifcopenshell==0.8.5
